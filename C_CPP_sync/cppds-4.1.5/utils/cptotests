#!/bin/sh
#
mv addtime tests
mv argarray tests
mv assoc tests
mv avltree tests
mv bcddigit tests
mv bits tests
mv bst tests
mv char tests
mv complex tests
mv deque tests
mv dlnklist tests
mv double tests
mv dynarray tests
mv function tests
mv fuzzyset tests
mv getpath tests
mv hashable tests
mv inifile tests
mv int_driver tests
mv integer tests
mv lonestr tests
mv longint tests
mv longnum tests
mv mcalc tests
mv objimp tests
mv point tests
mv primetbl tests
mv priqueue tests
mv project tests
mv queue tests
mv rational tests
mv rect tests
mv safeset tests
mv set tests
mv shortint tests
mv slnklist tests
mv slotfile tests
mv smartptr tests
mv sortlist tests
mv sorts tests
mv stack tests
mv statarray tests
mv strstore tests
mv tdate tests
mv testarray tests
mv testmatr tests
mv testpair tests
mv testpoly tests
mv testrand tests
mv timeofday tests
mv tristate tests
mv tsbtree tests
mv txtgraph tests
mv ulongint tests
mv unsigned tests

